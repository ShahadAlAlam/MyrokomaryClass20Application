CREATE TABLE "books" (
                         "id" integer PRIMARY KEY,
                         "title" varchar,
                         "author" varchar,
                         "publisher" varchar,
                         "edition" varchar,
                         "number_Of_Pages" integer,
                         "country" varchar,
                         "language" varchar
);
